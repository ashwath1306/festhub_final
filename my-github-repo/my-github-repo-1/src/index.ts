import { someUtilityFunction } from './lib/utils';

const initializeApp = () => {
    console.log('Application is starting...');
    // Call utility function as needed
    someUtilityFunction();
};

initializeApp();